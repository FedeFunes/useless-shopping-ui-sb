import { css } from "styled-components";

import { fontSizeNormal, fontWeightNormal } from "lib/theme";

export default css`
  ${fontSizeNormal}
  ${fontWeightNormal}
  font-family: "Work Sans", sans-serif;
`;
