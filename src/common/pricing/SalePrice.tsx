import styled from "styled-components";

import { fontSizeNormalBigger, fontWeightBold } from "lib/theme";

const SalePrice = styled.span`
  ${fontSizeNormalBigger}
  ${fontWeightBold}
  line-height: 1;
  letter-spacing: -0.32px;
`;

export default SalePrice;
